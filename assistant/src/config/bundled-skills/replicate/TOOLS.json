{
  "version": 1,
  "tools": [
    {
      "name": "replicate_run",
      "description": "Generate an image or video with a Replicate-hosted model and return the output media URL(s). Pass exactly two arguments: `model` (a string) and `input` (an object). For image generation, set `model` to 'black-forest-labs/flux-schnell' and `input` to { \"prompt\": \"<your description>\" }. Example call: { \"model\": \"black-forest-labs/flux-schnell\", \"input\": { \"prompt\": \"a single red circle on white\" } }. Do NOT put the prompt at the top level — it MUST go inside the `input` object.",
      "category": "media",
      "risk": "low",
      "input_schema": {
        "type": "object",
        "properties": {
          "model": {
            "type": "string",
            "description": "The Replicate model to run, as a single string. Use 'black-forest-labs/flux-schnell' for fast image generation (the default for images). Other valid forms: 'owner/name' (latest version), 'owner/name:version' (pinned version), or a bare 64-hex version hash. This is a string, NOT an object.",
            "examples": [
              "black-forest-labs/flux-schnell",
              "black-forest-labs/flux-1.1-pro",
              "stability-ai/stable-video-diffusion"
            ]
          },
          "input": {
            "type": "object",
            "description": "The model's input parameters, as an OBJECT. The text description MUST go under the `prompt` key — e.g. { \"prompt\": \"a golden retriever puppy in a field\" }. You may add other model-specific keys like `aspect_ratio`, `num_outputs`, `image` (an input image URL), or `seed`. Required key for image/video models: `prompt`.",
            "additionalProperties": true,
            "examples": [
              { "prompt": "a single red circle on white" },
              {
                "prompt": "a neon city skyline at night",
                "aspect_ratio": "16:9"
              }
            ]
          },
          "wait_seconds": {
            "type": "number",
            "description": "Optional. Maximum seconds to wait for completion (default 120, max 600). Increase for long video jobs. Leave unset for image generation."
          }
        },
        "required": ["model", "input"]
      },
      "executor": "tools/replicate-run.ts",
      "execution_target": "host"
    }
  ]
}
