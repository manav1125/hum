<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!--
      Embedded into the bare executable's __TEXT,__info_plist section by
      build-mac-helper.sh. TCC consults the requesting binary's Info.plist
      for usage strings when the helper itself (not just the responsible
      host app) is attributed for microphone / speech-recognition access,
      so the dictation-partials session can prompt and run without a full
      .app bundle.
    -->
    <key>CFBundleIdentifier</key>
    <string>ai.vellum.assistant.mac-helper</string>
    <key>CFBundleExecutable</key>
    <string>cue-mac-helper</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>Cue Helper</string>
    <!--
      User-facing name. CFBundleIdentifier stays as-is (TCC keys the mic/speech
      grants on the bundle id, and update/registration rely on it). The
      executable + bundle folder are named cue-mac-helper (the Electron main
      spawns the helper by that path). CFBundleName / CFBundleDisplayName are
      what the permission UIs — Privacy & Security → Accessibility, the
      mic/speech prompts — display, so both read "Cue Helper".
    -->
    <key>CFBundleDisplayName</key>
    <string>Cue Helper</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>1.0</string>
    <key>CFBundleVersion</key>
    <string>1</string>
    <key>NSMicrophoneUsageDescription</key>
    <string>Cue uses the microphone to transcribe dictated voice input.</string>
    <key>NSSpeechRecognitionUsageDescription</key>
    <string>Cue uses speech recognition to show your words live while you dictate.</string>
</dict>
</plist>
